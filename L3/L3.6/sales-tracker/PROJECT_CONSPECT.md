# Sales Tracker - Конспект для AI ассистента

## 🎯 Быстрый обзор проекта

**Sales Tracker** - CRUD веб-приложение для отслеживания продаж на Next.js 16 + Material-UI v7 + TypeScript.

**Путь проекта**: `L3/L3.6/sales-tracker/sales-tracker_web-client/`

## ⚡ Ключевые факты (для быстрого старта)

### Технологии:
- **Next.js 16.0.4** с App Router
- **TypeScript** + **Material-UI v7.3.5**
- **MaterialReactTable** для таблиц
- **pnpm** как пакетный менеджер

### Статус: ✅ Готов к работе
- Все компоненты UI реализованы
- API функции готовы (ожидают backend)
- Темная/светлая тема работает
- Формы с валидацией готовы

## 🏗️ Структура (что важно знать):

```
sales-tracker_web-client/
├── app/                    # Next.js страницы
│   ├── page.tsx           # Главная (3 кнопки вертикально)
│   ├── add-item/page.tsx  # Форма добавления
│   ├── items/page.tsx     # Таблица записей  
│   └── analytics/page.tsx # Аналитика с метриками
├── ui/                    # React компоненты
│   ├── Button.tsx         # MUI Button wrapper
│   ├── Card.tsx           # MUI Paper wrapper
│   ├── Input.tsx          # MUI TextField wrapper
│   ├── ThemeProvider.tsx  # Темная/светлая тема
│   ├── addItem/AddItemForm.tsx    # Форма добавления
│   ├── items/ItemsList.tsx        # Таблица с MRT
│   └── analytics/AnalyticsForm.tsx # Аналитика
└── libs/
    ├── types.ts           # TypeScript типы
    ├── theme.ts           # MUI темы
    └── api/sales.ts       # API функции (готовы!)
```

## 🔧 Быстрые команды:

```bash
cd L3/L3.6/sales-tracker/sales-tracker_web-client
pnpm install  # если нужно
pnpm run dev  # запуск на localhost:3000
```

## 📋 Что уже работает:

### ✅ Главная страница (`/`)
- 3 кнопки: "Добавить запись", "Посмотреть все записи", "Получить аналитику"
- Вертикальное расположение, max-width 300px
- Разные цвета: primary, secondary, success

### ✅ Форма добавления (`/add-item`)
- Поля: тип (доход/расход), категория, дата, сумма
- Валидация и обработка ошибок
- API: `POST /items`

### ✅ Таблица записей (`/items`)
- MaterialReactTable с сортировкой
- Редактирование (модальное окно)
- Удаление с подтверждением
- API: `GET/PUT/DELETE /items/:id`

### ✅ Аналитика (`/analytics`)
- Выбор периода (DatePicker)
- 5 метрик: сумма, средний чек, количество, медиана, 90-й перцентиль
- API: `GET /analytics?from&to`

### ✅ Темная/светлая тема
- Переключатель в Header
- Сохранение в localStorage
- Кастомные цветовые схемы

## 🎨 Дизайн:
- **Primary**: #6a1b9a (фиолетовый)
- **Secondary**: #ff9800 (оранжевый)  
- **Фон**: Однотонный (как в event-booker)
- Все компоненты - обертки над MUI

## 🔗 API (готово, ждет backend):

```typescript
// Ожидаемые endpoints:
POST /items              // Создать запись
GET /items               // Получить список
PUT /items/:id           // Обновить запись  
DELETE /items/:id        // Удалить запись
GET /analytics?from&to   // Аналитика за период
GET /items/export        // CSV экспорт
```

**Переменная окружения**: `NEXT_PUBLIC_API_URL=http://localhost:8080`

## ⚠️ Что нужно доделать:

1. **Backend интеграция** - Go сервер в `sales-tracker_main-server/`
2. **Обработка ошибок** - добавить toast уведомления
3. **Загрузочные состояния** - skeleton loaders
4. **Тестирование** - unit и e2e тесты

## 🚀 Следующие шаги (типичные задачи):

- Подключить backend API
- Добавить обработку ошибок от сервера
- Улучшить UX (уведомления, состояния загрузки)
- Добавить тесты
- Оптимизировать performance

## 💡 Быстрые решения типичных проблем:

### Если не запускается:
```bash
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

### Если MUI ошибки:
- Проверить что установлены все Emotion пакеты
- Убедиться что Geist шрифт не дублируется

### Если API не работает:
- Проверить NEXT_PUBLIC_API_URL
- Backend должен быть запущен на 8080 порту

---

**Статус**: Проект готов к интеграции с backend. Все UI компоненты и логика реализованы.