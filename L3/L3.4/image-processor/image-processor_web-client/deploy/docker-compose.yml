services:
  image-processor_web-client:
    image: ghcr.io/golovanevvs/image-processor_web-client:${APP_VERSION:-latest}
    container_name: ${WEB_CLIENT_CONTAINER}
    environment:
      - NODE_ENV=production
      - PORT=${PORT}
    networks:
      - proxy

networks:
  proxy:
    external: true